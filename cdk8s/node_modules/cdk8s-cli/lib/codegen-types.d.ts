import { JSONSchema4 } from "json-schema";
import { CodeMaker } from "codemaker";
export declare class TypeGenerator {
    private readonly schema;
    private readonly emitLater;
    private readonly emitted;
    constructor(schema?: JSONSchema4);
    addType(typeName: string, def: JSONSchema4): void;
    generate(code: CodeMaker): void;
    private emitUnion;
    private emitStruct;
    private emitDescription;
    private typeForProperty;
    private typeForObject;
    private typeForRef;
    private typeForArray;
    private resolveReference;
}
